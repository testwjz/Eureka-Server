server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: feign-consumer

ribbon:
  eureka:
    enable: true

feign:
  hystrix:
    enabled: true