server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
      - id: 163_route
        uri: http://www.163.com/
        predicates:
        - Path=/163
      - id: spring_route
        uri: https://spring.io/projects/
        predicates:
        - Path=/spring